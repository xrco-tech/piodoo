[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
; TLS for WebRTC (wss) — cert/key mounted at /etc/asterisk/keys.
tlsenable = yes
tlsbindaddr = 0.0.0.0:8089
tlscertfile = ${WSS_CERT}
tlsprivatekey = ${WSS_KEY}
