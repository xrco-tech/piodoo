[modules]
autoload=yes
; res_pjsip_transport_websocket declines to load unless res_http_websocket is
; already up — preload it so the WebRTC (wss) PJSIP transport initialises.
preload=res_http_websocket.so
