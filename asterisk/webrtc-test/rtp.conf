[general]
rtpstart = 10000
rtpend = 10200
icesupport = yes
; For a LAN test the box's own interface IP is a reachable ICE candidate.
; Across NAT, add a STUN server (or use coturn) so ICE can gather candidates:
; stunaddr = stun.l.google.com:19302
